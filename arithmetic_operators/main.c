#include <stdio.h>

int main(){

    int x = 10;
    int y = 3;

    int z = 0;

    // you have to devide by a floating point number to retain the decimal portion
    //z = x+y;
    //z = x-y;
    // z = x*y;
    // z = x/y;
    // z = x%y;
    //good for checking if the number is divided by 2 or not
    // x--;

    //x = x + 2;
    //x+=3;

    // x-=3;
    // x*=2;

    printf("%d", x);
}
