#include <stdio.h>

int main(){

    //variable = A reusable container for a value
    //it behaves like it is the values it contains

    //integer variable

    int age = 10;

    printf("You are %d years old\n", age);

    int year = 2025;

    printf("The year is %d\n", year);

    int grade = 12;

    printf("I am in grade %d\n", grade);

    int quantity = 5;

    printf("You have ordered %dx items", quantity);

    return 0;
}